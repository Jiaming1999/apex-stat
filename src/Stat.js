import React from 'react';
import StatCard from './StatCard';

const Stat = (props) => {
    const {metadata} = props;

    return(
      <div>
        Stat
      </div>
    )

};

export default Stat;